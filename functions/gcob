gcob() {
  local branches branch
  branches=$(git branch -vv --color=always) &&
  branch=$(echo "$branches" | fzf-tmux -d 15 --ansi) &&
  git checkout $(echo "$branch" | awk '{print $1}' | sed "s/.* //")
}


